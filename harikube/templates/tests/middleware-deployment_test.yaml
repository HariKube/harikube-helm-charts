suite: test middleware deployment rendering
templates:
  - middleware-deployment.yaml
tests:
  - it: should render correct replica count
    set:
      replicaCount: 1
    asserts:
      - equal:
          path: spec.replicas
          value: 1