apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: countresponses.api-extension.harikube.info
spec:
  group: api-extension.harikube.info
  names:
    kind: CountResponse
    listKind: CountResponseList
    plural: countresponses
    singular: countresponse
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.count
      name: Count
      type: integer
    name: v1
    schema:
      openAPIV3Schema:
        description: CountResponse is the Schema for the countresponses API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of CountResponse
            properties:
              count:
                format: int64
                type: integer
            required:
            - count
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: transactionrequests.api-extension.harikube.info
spec:
  group: api-extension.harikube.info
  names:
    kind: TransactionRequest
    listKind: TransactionRequestList
    plural: transactionrequests
    singular: transactionrequest
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: TransactionRequest is the Schema for the Transactions API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Transaction
            properties:
              create:
                items:
                  format: byte
                  type: string
                type: array
              delete:
                items:
                  format: byte
                  type: string
                type: array
              update:
                items:
                  format: byte
                  type: string
                type: array
            required:
            - create
            - delete
            - update
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: transactionresponses.api-extension.harikube.info
spec:
  group: api-extension.harikube.info
  names:
    kind: TransactionResponse
    listKind: TransactionResponseList
    plural: transactionresponses
    singular: transactionresponse
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.error
      name: Error
      type: string
    name: v1
    schema:
      openAPIV3Schema:
        description: TransactionResponse is the Schema for the Transactions API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the actual state of Transaction
            properties:
              error:
                type: string
            required:
            - error
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources: {}
